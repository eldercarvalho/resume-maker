import styled from 'styled-components';

export const Container = styled.h3`
  button {
    width: 100%;
    border: none;
    background: none;
    font-size: 1.6rem;
    padding: 1.2rem;
    text-align: left;
  }
`;
